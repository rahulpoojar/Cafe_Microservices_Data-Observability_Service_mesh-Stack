apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
    release: prometheus
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.auth.port }}"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: {{ .Values.auth.port }}
      targetPort: {{ .Values.auth.port }}
