{{- $svc := .Values.microservices["cart-service"] -}}

---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: cart-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service

  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
      - name: cart-service
        image: {{ $svc.image }}
        ports:
        - containerPort: {{ $svc.port }}
        env:
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "{{ $.Values.global.otelEndpoint }}"
        - name: OTEL_SERVICE_NAME
          value: "cart-service"

  strategy:
    canary:
      stableService: cart-service-stable
      canaryService: cart-service-canary
      steps:
      - setWeight: 20
      - pause: { duration: 20s }
      - setWeight: 60
      - pause: { duration: 20s }
      - setWeight: 100

---
apiVersion: v1
kind: Service
metadata:
  name: cart-service-stable
spec:
  selector:
    app: cart-service
    rollouts-pod-template-hash: stable
  ports:
  - port: {{ $svc.port }}
    targetPort: {{ $svc.port }}

---
apiVersion: v1
kind: Service
metadata:
  name: cart-service-canary
spec:
  selector:
    app: cart-service
    rollouts-pod-template-hash: canary
  ports:
  - port: {{ $svc.port }}
    targetPort: {{ $svc.port }}
